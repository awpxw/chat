server:
  port: 8080
spring:
  application:
    name: gateway
  main:
    web-application-type: reactive
  cloud:
    nacos:
      discovery:
        server-addr: ${SERVER:192.168.209.136}:8848
        username: nacos
        password: nacos
        namespace: dev
      config:
        import-check:
          enabled: false
        server-addr: ${SERVER:192.168.209.136}:8848
        username: nacos
        password: nacos
        file-extension: yaml
        namespace: dev
        group: DEFAULT_GROUP
        refresh-enabled: true
        shared-configs:
          - data-id: common.yaml
            group: DEFAULT_GROUP
            refresh: true