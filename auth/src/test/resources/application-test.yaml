server:
  port: 8081

spring:
  application:
    name: auth
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.91.129:8848
        namespace: 47bc373d-81e6-4dac-b494-45132a4d88d4
        ephemeral: true
    service-registry:
      auto-registration:
        enabled: true

management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: health
  endpoint:
    health:
      show-details: always