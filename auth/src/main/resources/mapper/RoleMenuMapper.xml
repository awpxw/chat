<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aw.mapper.RoleMenuMapper">

    <insert id="insertBatch">
        INSERT INTO t_role_menu
        (id, role_id, menu_id, status, create_user, create_time, update_user, update_time, deleted)
        VALUES
        <foreach collection="roleMenus" item="item" separator=",">
            (#{item.id}, #{item.roleId}, #{item.menuId}, 1, #{userId}, NOW(), #{userId}, NOW(), 0)
        </foreach>
    </insert>

</mapper>