<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aw.mapper.UserRoleMapper">

    <insert id="insertBatch">
        INSERT INTO t_user_role
        (id,user_id, role_id, status, create_user, create_time, update_user, update_time, deleted)
        VALUES
        <foreach collection="userRoles" item="item" separator=",">
            (#{item.id},#{item.userId}, #{item.roleId}, 1, #{loginUserId}, NOW(), #{loginUserId}, NOW(), 0)
        </foreach>
    </insert>

</mapper>