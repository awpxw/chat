spring:
  application:
    name: auth

  cloud:
    nacos:
      discovery:
        server-addr: 192.168.91.129:8848
        username: nacos
        password: nacos


      config:
        import-check:
          enabled: false
        server-addr: 192.168.91.129:8848
        username: nacos
        password: nacos
        file-extension: yaml
        namespace: ${spring.profiles.active}
        group: DEFAULT_GROUP
        refresh-enabled: true
        shared-configs:
          - data-id: common.yaml
            refresh: true