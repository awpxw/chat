<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aw.mapper.MessageMapper">

    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO t_message (
        id,
        conversation_id,
        sender_id,
        msg_type,
        content,
        extra,
        msg_time,
        status,
        create_time,
        create_user,
        create_user_name,
        update_time,
        update_user,
        update_user_name,
        deleted
        ) VALUES
        <foreach collection="list" item="item" index="index" separator=",">
            (
            #{item.id},
            #{item.conversationId},
            #{item.senderId},
            #{item.msgType},
            #{item.content},
            #{item.extra},
            #{item.msgTime},
            7,
            NOW(),
            #{user.userId},
            #{user.userName},
            NOW(),
            #{item.userId},
            #{item.userName},
            0
            )
        </foreach>
    </insert>

</mapper>